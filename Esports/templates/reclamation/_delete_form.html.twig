<form method="post" style="display: inline-block" action="{{ path('reclamation_delete', {'id': reclamation.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.id) }}">
    <button style="background-color: transparent"><a class="bi bi-trash white"></a></button>
</form>
