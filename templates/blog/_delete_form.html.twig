<form method="post" action="{{ path('blog_delete', {'id': blog.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog.id) }}">
    <button class="btn" style="padding: 0;"><a class="bi bi-trash"></a></button>
</form>
